<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}ServiGo{% endblock %}</title>

    <style>
        /* ===== STYLES GÉNÉRAUX ===== */
        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #ff6b6b 0%, #ffa500 100%);
            min-height: 100vh;
            color: #333;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
        }

        /* Header */
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 0;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 15px;
            font-size: 2.5rem;
            font-weight: bold;
            color: #333;
            text-decoration: none;
        }

        .logo-img {
        width: 100px;  /* Largeur souhaitée */
        height: auto; /* Garde les proportions */
        }

        .nav-links {
            display: flex;
            gap: 40px;
            align-items: center;
        }

        .nav-link {
            color: white;
            text-decoration: none;
            font-size: 1.2rem;
            font-weight: 500;
            transition: opacity 0.3s ease;
        }

        .nav-link:hover {
            opacity: 0.8;
        }
        .nav-links a {
            display: inline-block;
            margin-left: 12px;
            padding: 10px 24px;
            border-radius: 30px;
            font-weight: 600;
            font-size: 1rem;
            text-decoration: none;
            transition: all 0.3s ease;
        }

        /* Bouton principal (violet / bleu dégradé) */
        .btn-primary {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            box-shadow: 0 6px 20px rgba(102, 126, 234, 0.3);
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.4);
        }

        /* Bouton secondaire (contour violet) */
        .btn-secondary {
            background: transparent;
            color: #667eea;
            border: 2px solid #667eea;
        }

        .btn-secondary:hover {
            background: #667eea;
            color: white;
        }

        /* Bouton neutre (blanc, par exemple pour Espace client) */
        .btn {
            background: white;
            color: #2d3436;
            border: 1px solid #dfe6e9;
        }

        .btn:hover {
            background: #f1f3f5;
        }

        /* Bouton Déconnexion (rouge doux) */
        .btn-logout {
            background: #ff6b6b;
            color: white;
            border: none;
            box-shadow: 0 6px 20px rgba(255, 107, 107, 0.3);
        }

        .btn-logout:hover {
            background: #ff5252;
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(255, 82, 82, 0.4);
        }

        /* Responsive */
        @media (max-width: 768px) {
            .header {
                flex-direction: column;
                gap: 20px;
                text-align: center;
            }

            .nav-links {
                flex-wrap: wrap;
                justify-content: center;
                gap: 20px;
            }
        }

        @media (max-width: 480px) {
            .logo {
                font-size: 2rem;
            }
        }

        {% block stylesheets %}{% endblock %}
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <header class="header">
<a href="{{ path('app_home') }}" class="logo">
    <img src="{{ asset('images/ServiGo.png') }}" alt="Logo ServiGo" class="logo-img">
</a>

<nav class="navbar">
    <div class="container">
        <a href="{{ path('app_home') }}" class="logo"></a>

        <div class="nav-links">
            {% if app.user %}
                {% if is_granted('ROLE_PRESTATAIRE') %}
                    <a href="{{ path('app_account_prestataire') }}" class="btn">Espace prestataire</a>
                {% elseif is_granted('ROLE_CLIENT') %}
                    <a href="{{ path('app_account_client') }}" class="btn">Espace client</a>
                {% endif %}

                <a href="{{ path('app_logout') }}" class="btn">Déconnexion</a>
            {% else %}
                <a href="{{ path('app_register') }}" class="btn">S’inscrire</a>
                <a href="{{ path('app_login') }}" class="btn btn-secondary">Connexion</a>
                <a href="{{ path('app_prestataire') }}" class="btn btn-secondary">prestataire</a>
            {% endif %}
        </div>
    </div>
</nav>
        </header>

        <!-- Contenu principal -->
        {% block body %}{% endblock %}
    </div>

    {% block javascripts %}{% endblock %}
</body>
</html>